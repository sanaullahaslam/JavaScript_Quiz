const array = [10, 20, 30, 40, 50];

const result = array.map((value, index) => `Value at index ${index} is ${value}`);

result.forEach((item) => {

    console.log(item);

                        }
              );
